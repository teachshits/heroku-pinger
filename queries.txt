Website.select("id, url, minute, name")

Website.select("id, url, minute, name").where("user_id = ?", 3 )
Website.select([:id, :url, :minute, :name]).where("user_id = ?", 3 )


Website.select(:minute)

Website.select([:minute, :id])

uu = User.where("id = ?", 4) gives  a ActiveRecord::Relation
To get an instance of the User class, do this:
uu = User.where("id = ?", 6).first
