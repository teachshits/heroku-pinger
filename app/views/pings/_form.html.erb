<%= form_for(@ping) do |f| %>
  <% if @ping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ping.errors.count, "error") %> prohibited this ping from being saved:</h2>

      <ul>
      <% @ping.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br />
    <%= f.number_field :status %>
  </div>
  <div class="field">
    <%= f.label :response_length %><br />
    <%= f.number_field :response_length %>
  </div>
  <div class="field">
    <%= f.label :website_id %><br />
    <%= f.number_field :website_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
